Ок, даю «пошаговку» без воды. Сразу замечание по корректности: если не учитывать мультивкладки, счётчик собьётся. Минимум — добавь `presence:user:{userId}` (SET из socketId) как внутренний тех.ключ для корректного добавления/удаления `userId` в глобальные/чатовые SET’ы. Это общепринятая практика при масштабировании Socket.IO через Redis-адаптер. ([socket.io][1])

---

# Ключи (итог)

* `presence:users` — SET `<userId>` (глобально онлайн).
* `presence:chat:{chatId}` — SET `<userId>` (онлайн по чату).
* `profile:{userId}` — RedisJSON с визиткой (`username`, `avatarUrl`, и т.д.). ([Redis][2])
* Тех.ключ (очень рекомендуется): `presence:user:{userId}` — SET `<socketId>`.

SCARD для подсчётов — O(1). ([Redis][3])

---

# Сценарии (Backend ↔ Frontend)

## A) Клиент устанавливает сокет-соединение (глобальный онлайн)

**Backend**

1. Валидирует токен; кладёт пользователя в `socket.data.user`.
2. `SADD presence:user:{userId} socketId`. Если это **первый** сокет юзера (`SCARD == 1`) → `SADD presence:users userId`.
3. Считает `total = SCARD presence:users` и эмитит `ONLINE_TOTAL_UPDATED { total }` в общую комнату (например, `stats`).
   **Почему так:** корректно учитываем несколько вкладок одного юзера. Масштабирование — через Redis adapter + sticky sessions. ([socket.io][1])

**Frontend**

1. Подключается к сокету.
2. Подписывается на `ONLINE_TOTAL_UPDATED` (для хедера/глобального счетчика).
3. Для первой отрисовки может сделать REST: `GET /presence/summary` → `{ total }` (сокет-дельты пойдут дальше).

---

## B) Клиент заходит в конкретный чат (presence по чату)

**Frontend**

1. Шлёт `JOIN_CHAT { chatId }`.
2. Сразу делает REST-снимок: `GET /chats/{chatId}/presence` → `{ memberIds: string[], count: number }`.

    * Если нужно — второй запрос батч-профилей: `GET /users?ids=...` → `{ users: Profile[] }`.

**Backend**

1. Проверяет доступ → `socket.join("chat:{id}")`.
2. Помечает присутствие:

    * `SADD presence:chat:{id} userId` **только если** это первый сокет юзера в этом чате (отслеживается через тех.ключ, напр. `presence:chat:user:{id}:{userId}` = SET `<socketId>`).
3. Рассылает дельту в `chat:delta:{id}`:
   `CHAT_MEMBER_JOINED { chatId, userId }` (+ опционально `profile` из `profile:{userId}`).
4. Считает `count = SCARD presence:chat:{id}` и шлёт `CHAT_ONLINE_UPDATED { chatId, count }`.

---

## C) Как отдать **первый список онлайн-пользователей**

**Backend (REST)**

* `GET /chats/{id}/presence`:

    * Берёт `memberIds = SMEMBERS presence:chat:{id}`.
    * Возвращает `{ memberIds, count: memberIds.length }`.
    * (Опционально: вернуть ещё `{ profiles }`, если хочешь «обогащённый» снимок без второго запроса — но следи за размером ответа.)

**Frontend**

* После монтирования списка/чата: вызывает этот REST.
* Для неизвестных `userId` добирает визитки батч-запросом `/users?ids=...` и кэширует локально.

> Это «snapshot + дельты» — стандартная модель в presence-каналах (см. Pusher). ([pusher.com][4])

---

## D) Что слать при изменениях: **дельты** или **весь список**?

**Рекомендация** — **дельты**:

* На join: `CHAT_MEMBER_JOINED { chatId, userId }` (+ опционально `profile`).
* На leave: `CHAT_MEMBER_LEFT { chatId, userId }`.
* Отдельно — `CHAT_ONLINE_UPDATED { chatId, count }` (быстрая метрика).

**Когда слать полный список**:

* Только при **первом подключении** к чату (см. B/C) или при рассинхронизации (см. F — recovery): отправь `CHAT_ONLINE_SNAPSHOT { chatId, memberIds, count }`.

Плюсы дельт: маленькие payload’ы и масштабирование. Это тот же принцип у managed-провайдеров presence. ([pusher.com][4])

---

## E) Клиент уходит из чата / закрывает вкладку / disconnect

**Frontend**

* Явный уход: шлёт `LEAVE_CHAT { chatId }` (и/или уходит со страницы).
* Закрытие вкладки: `disconnect` происходит автоматически.

**Backend**

1. `LEAVE_CHAT`:

    * `SREM presence:chat:user:{id}:{userId} socketId`.
    * Если у юзера **не осталось** сокетов в этом чате → `SREM presence:chat:{id} userId` →
      `CHAT_MEMBER_LEFT { chatId, userId }` и обновление `count`.
2. `disconnect`:

    * `SREM presence:user:{userId} socketId`; если `SCARD == 0` → `SREM presence:users userId` и `ONLINE_TOTAL_UPDATED`.
    * Для чатов, где висел этот `socketId`: шаги как при `LEAVE_CHAT`.

---

## F) Потеря событий / ресинк (recovery)

Редко, но бывает (переподключение, ребаланс нод). Сделай лёгкий механизм:

* Сервер держит `presence:chat:{id}:version` (INT), инкрементит при изменениях.
* В дельтах отправляй `version`.
* Клиент хранит последнюю `version`. Если получает «дырку» (пропущенная версия) — делает `GET /chats/{id}/presence` и принимает `CHAT_ONLINE_SNAPSHOT`.
  Это дешёво и надёжно на кластере (Socket.IO + Redis adapter). ([socket.io][1])

---

## G) Профили (username, avatar) — когда и как подмешивать

* **Снимок**: фронт берёт `memberIds` и делает батч `/users?ids=...` (сервер читает из `profile:{userId}` через RedisJSON и только при промахе — БД). ([Redis][2])
* **Дельты**: можно присылать `profile` **только** для `JOINED` (новый участник), чтобы не плодить лишний трафик.
* При изменении профиля: эмитить `USER_PROFILE_UPDATED { userId, profile }` — фронт обновит локальный кэш.

---

## H) Масштабирование и производительность

* Поднимай `@socket.io/redis-adapter` + включай sticky sessions (нужно при long-polling). ([socket.io][1])
* Счётчики — только через Redis (`SCARD`), не через `fetchSockets()`/межузловые ack-запросы. Это быстрее и стабильнее под нагрузкой. ([Redis][3])
* Пабсаб/шардированный Pub/Sub (Redis 7) поддерживается адаптером, если упрёшься в throughput. ([GitHub][5])

---

# Короткий ответ на твои вопросы

**1) «Как юзер получает первый список онлайн пользователей?»**
Сразу после `JOIN_CHAT` фронт делает `GET /chats/{chatId}/presence` → `{ memberIds, count }` (+ при необходимости батч профилей `/users?ids=...`). Это и есть «первый снимок». Далее — дельты по сокету.

**2) «При изменениях слать конкретного пользователя или весь список?»**
Дельты: `JOINED/LEFT` по одному `userId` (+ `count` отдельным событием). Полный список — только на старт/ресинк. Это best practice для presence-каналов и масштабируемых чатов. ([pusher.com][4])

---

Если нужно, накину компактные контракты REST/Socket-событий и схему ключей для тех.учёта сокетов (`presence:chat:user:{chatId}:{userId}`, `presence:by-socket:{socketId}`) — без кода, но так, чтобы ты сразу встроил в свой gateway.

[1]: https://socket.io/docs/v4/redis-adapter/?utm_source=chatgpt.com "Redis adapter | Socket.IO"
[2]: https://redis.io/blog/redis-as-a-json-store/?utm_source=chatgpt.com "RedisJSON: A Redis JSON Store | Redis"
[3]: https://redis.io/docs/latest/commands/scard/?utm_source=chatgpt.com "SCARD | Docs"
[4]: https://pusher.com/blog/what-are-presence-channels-pusher-presence-channels-when-and-how-to-use-them/?utm_source=chatgpt.com "What are Presence channels? Pusher Presence channels: when and how to use them | Pusher blog"
[5]: https://github.com/socketio/socket.io-redis-adapter?utm_source=chatgpt.com "GitHub - socketio/socket.io-redis-adapter: Adapter to enable broadcasting of events to multiple separate socket.io server nodes."


Вот логичный и полный набор событий, которых тебе не хватает для корректной реализации **онлайна и presence-системы**.

---

# ✅ Что уже есть

Ты уже используешь:

**Client → Server**

* `JOIN_CHAT`
* `LEAVE_CHAT`
* `JOIN_CHAT_DELTA`
* `LEAVE_CHAT_DELTA`

**Server → Client**

* `ME_JOINED_CHAT`
* `ME_LEFT_CHAT`
* `CHAT_MESSAGE_NEW`
* `CHAT_DELTA_NEW`

Эти события закрывают взаимодействие пользователя с чатом и получение сообщений, но **не покрывают online/presence-состояние**.

---

# ✅ Какие события нужно добавить (минимальный набор)

### 📌 1. Глобальный онлайн (все пользователи)

| Направление         | Событие                | Когда используется                                                                                 |
| ------------------- | ---------------------- | -------------------------------------------------------------------------------------------------- |
| **Server → Client** | `ONLINE_TOTAL_UPDATED` | Отправляется всем подключённым пользователям при изменении общего количества онлайн-пользователей. |

**Payload:**

```ts
{
  total: number;      // сколько всего уникальных пользователей онлайн
}
```

---

### 📌 2. Онлайн внутри чата (список участников)

| Направление         | Событие                | Для чего                                                                            |
| ------------------- | ---------------------- | ----------------------------------------------------------------------------------- |
| **Server → Client** | `CHAT_ONLINE_SNAPSHOT` | Полный список юзеров онлайн в чате — отправляется только при входе/переподключении. |
| **Server → Client** | `CHAT_MEMBER_JOINED`   | Кто-то зашёл в чат → добавить одного.                                               |
| **Server → Client** | `CHAT_MEMBER_LEFT`     | Кто-то ушёл из чата → убрать одного.                                                |
| **Server → Client** | `CHAT_ONLINE_UPDATED`  | Только число участников без списка.                                                 |

**Payload примеры:**

```ts
// CHAT_ONLINE_SNAPSHOT
{
  chatId: string;
  members: string[]; // userIds
}

// CHAT_MEMBER_JOINED
{
  chatId: string;
  userId: string;
  profile?: { username: string; avatarUrl: string }; // Можно сразу
}

// CHAT_MEMBER_LEFT
{
  chatId: string;
  userId: string;
}

// CHAT_ONLINE_UPDATED
{
  chatId: string;
  count: number;
}
```

---

### 📌 3. Профиль пользователя обновился

| Направление         | Событие                | Зачем                                                                      |
| ------------------- | ---------------------- | -------------------------------------------------------------------------- |
| **Server → Client** | `USER_PROFILE_UPDATED` | Если юзер сменил username или аватар, остальным в чатах нужно обновить UI. |

**Payload:**

```ts
{
  userId: string;
  profile: {
    username: string;
    avatarUrl: string;
  }
}
```

---

# ✅ Полный Enum (как мог бы выглядеть)

```ts
export enum ServerToClientEvent {
  APP_ERROR = 'app:error',
  CHAT_ERROR = 'chat:error',

  ME_JOINED_CHAT = 'me:joined:chat',
  ME_LEFT_CHAT = 'me:left:chat',

  CHAT_MESSAGE_NEW = 'chat:message:new',
  CHAT_DELTA_NEW = 'chat:delta:new',

  // 👇 Новое для онлайна
  ONLINE_TOTAL_UPDATED = 'online:total:updated',

  CHAT_ONLINE_SNAPSHOT = 'chat:online:snapshot',
  CHAT_ONLINE_UPDATED = 'chat:online:updated',

  CHAT_MEMBER_JOINED = 'chat:member:joined',
  CHAT_MEMBER_LEFT = 'chat:member:left',

  USER_PROFILE_UPDATED = 'user:profile:updated',
}
```

Клиенту ничего нового слать не нужно — всё обновление идёт **Server → Client**.

---

# ✅ Итог

| Что                           | Нужно? | Событие                |
| ----------------------------- | ------ | ---------------------- |
| Общий онлайн                  | ✅      | `ONLINE_TOTAL_UPDATED` |
| Первый список участников чата | ✅      | `CHAT_ONLINE_SNAPSHOT` |
| Кто зашёл                     | ✅      | `CHAT_MEMBER_JOINED`   |
| Кто вышел                     | ✅      | `CHAT_MEMBER_LEFT`     |
| Только число                  | ✅      | `CHAT_ONLINE_UPDATED`  |
| Обновление профиля            | ✅      | `USER_PROFILE_UPDATED` |

---

Хочешь — могу дальше расписать payload'ы интерфейсами TypeScript или написать handlers на стороне Nest.js. Напиши «давай код».
